language: python

sudo: false

python:
    - '3.4'

install:
    - pip install --upgrade pip
    - pip install pep8 nose coverage sphinx

script:
    - pep8 pyardrone
    - nosetests --with-coverage --cover-package pyardrone
    - cd docs && make html
